import React from 'react';

import styles from './styles';

import { Text } from 'react-native';

const Sections = () => {
    return (
        <Text style={{color: '#fff'}}>Seção</ Text>
    );
};

export default Sections;